{
  "buildCommand": "npx prisma migrate deploy && npx prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["bom1"],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://college-finder-z9dq.onrender.com/api/$1"
    }
  ]
}
